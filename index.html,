<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>KAT EMPIRE: GLOBAL</title>
    <style>
        /* Estilo Retro RuneScape */
        body { background-color: #1a1a1a; color: #ffd700; font-family: 'Courier New', monospace; margin: 0; text-align: center; overflow-x: hidden; }
        .barra-stats { background: #2b2b2b; padding: 10px; border-bottom: 3px solid #5d4037; display: flex; justify-content: space-around; font-size: 14px; font-weight: bold; }
        .mapa-img { width: 100%; border: 2px solid #5d4037; display: block; box-shadow: inset 0 0 20px #000; }
        .consola { background: #111; color: #0f0; font-size: 12px; height: 80px; overflow-y: auto; padding: 10px; text-align: left; border-bottom: 3px solid #5d4037; line-height: 1.4; }
        .menu-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; padding: 15px; background: #3e2723; }
        button { background: #4e342e; color: #ffd700; border: 2px solid #ffd700; padding: 15px; font-family: 'Courier New'; font-weight: bold; font-size: 13px; cursor: pointer; text-transform: uppercase; box-shadow: 3px 3px #000; }
        button:active { background: #331d19; transform: translate(2px, 2px); box-shadow: none; }
        .pixel-txt { text-shadow: 2px 2px #000; }
    </style>
</head>
<body>
    <div class="barra-stats">
        <span class="pixel-txt">💰 KAT: 148,000</span>
        <span class="pixel-txt">⚔️ NIVEL: 15</span>
        <span class="pixel-txt">🌍 GLOBAL</span>
    </div>

    <img src="https://i.imgur.com/n6t3fXh.jpeg" class="mapa-img" alt="Mapa Mundial KAT Empire">

    <div class="consola" id="log">
        - Bienvenido al territorio del Imperio.<br>
        - ¡El mapa mundial está listo para la conquista!<br>
        - Sistema de KAT Coins activo...
    </div>

    <div class="menu-grid">
        <button onclick="registrar('⚔️ Iniciando batalla en zona cercana...')">⚔️ LUCHAR</button>
        <button onclick="registrar('⚒️ Accediendo a la Forja Imperial...')">⚒️ LA FORJA</button>
        <button onclick="registrar('🏴‍☠️ Entrando al Mercado Negro...')">🏴‍☠️ MERCADO</button>
        <button onclick="registrar('💳 Cargando sistema de Retiro...')">💳 RETIRO</button>
    </div>

    <script>
        function registrar(msg) {
            const log = document.getElementById('log');
            log.innerHTML += `<br>- ${msg}`;
            log.scrollTop = log.scrollHeight;
        }
    </script>
</body>
</html>
